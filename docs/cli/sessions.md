---
summary: "CLI reference for `dmms-ai sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `dmms-ai sessions`

List stored conversation sessions.

```bash
dmms-ai sessions
dmms-ai sessions --active 120
dmms-ai sessions --json
```
