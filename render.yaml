services:
  - type: web
    name: dryads-ai
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "8080"
      - key: SETUP_PASSWORD
        sync: false
      - key: DRYADS_AI_STATE_DIR
        value: /data/.dryads-ai
      - key: DRYADS_AI_WORKSPACE_DIR
        value: /data/workspace
      - key: DRYADS_AI_GATEWAY_TOKEN
        generateValue: true
    disk:
      name: dryads-ai-data
      mountPath: /data
      sizeGB: 1
